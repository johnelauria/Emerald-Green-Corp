<%= form_for(@emailu) do |f| %>
  <% if @emailu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@emailu.errors.count, "error") %> prohibited this emailu from being saved:</h2>

      <ul>
      <% @emailu.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="alert alert-info">
    <button type="button" class="close" data-dismiss="alert">x</button>
    <p>For testing purposes, message will be sent to the email provided in the email form...</p>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email, placeholder: "We will respond in this email" %>
  </div>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message, placeholder: "Enter your message here and we'll respond to you as soon as we receive your message", rows: 5 %>
  </div>
  <div class="actions">
    <%= f.submit "send", class: "btn btn-info" %>
  </div>
<% end %>
