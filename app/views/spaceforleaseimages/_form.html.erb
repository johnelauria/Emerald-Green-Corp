<%= form_for(@spaceforleaseimage) do |f| %>
  <% if @spaceforleaseimage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spaceforleaseimage.errors.count, "error") %> prohibited this spaceforleaseimage from being saved:</h2>

      <ul>
      <% @spaceforleaseimage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<%= f.hidden_field :spaceforlease_id, value: @spaceforlease.id %>
  <div class="field">
    <%= f.label :image %><br />
    <%= f.file_field :image %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, rows: 5 %>
  </div>
  <div class="actions">
    <%= f.submit "Add image", class: "btn btn-success" %>
  </div>
<% end %>
