<%= form_for(@clientprofile) do |f| %>
  <% if @clientprofile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clientprofile.errors.count, "error") %> prohibited this clientprofile from being saved:</h2>

      <ul>
      <% @clientprofile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content, rows: 7 %>
  </div>

<%= f.hidden_field :user_id, value: @user.id %>

  <div class="actions">
    <%= f.submit "Save", class: "btn btn-success" %>
  </div>
<% end %>
