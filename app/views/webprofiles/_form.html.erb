<%= form_for(@webprofile) do |f| %>
  <% if @webprofile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@webprofile.errors.count, "error") %> prohibited this webprofile from being saved:</h2>

      <ul>
      <% @webprofile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content, rows: 10 %>
  </div>
  <div class="actions">
    <%= f.submit "Save", class: "btn btn-success" %>
  </div>
<% end %>
